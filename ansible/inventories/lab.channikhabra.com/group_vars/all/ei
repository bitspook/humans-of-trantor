ei_version:                  "{{ hot_version }}"

ei_dockerfile:        "Dockerfile-prod"
ei_docker_name:       "hot-ei"
ei_docker_tag:        "{{ ei_version }}"

ei_docker_registry:   "{{ hot_docker_registry }}"
ei_docker_repository: "{{ ei_docker_registry + '/' + ei_docker_name if ei_docker_registry else '' }}"

ei_docker_registry_username: "{{ hot_docker_registry_username }}"
ei_docker_registry_password: "{{ hot_docker_registry_password }}"
